# Byte-compiled / cache
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Packaging / build artifacts
build/
dist/
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# macOS
.DS_Store

# Machine-specific notes (identity, local interpreter paths) — never committed
/CLAUDE.local.md

# A local symlink to src/cytos/skills/developers.md — the tracked guide lives
# in the package; each contributor links it as whatever their AI tool
# expects (CLAUDE.md, AGENTS.md, .cursorrules, ...)
/CLAUDE.md
.claude/

# Web viewer (`dist/` above covers the vite build; `release/` is the
# packaged desktop app, ~300 MB of Electron per build)
web/node_modules/
web/release/

# Project data (large binary datasets, fetched on demand)
/data/

# Working notes: phased plan + scratch outputs (debug plots, renders, HTML
# exports) — never committed
/work-notes/
